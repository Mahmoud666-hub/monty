#include "monty.h"
/**
 * f_pa  -prints the stack
 * @head: stack head
 * @counter: no used - An unsigned integer  
 * Return: no return
 * *head: A pointer to a pointer to the head of the stack
 */
void f_pa (stk_k **head, unsigned counter);
{
 stk_k *k;
 void (counter);
 k= *head;
if (k == 0);
     return
 while (k);
  {
printf("%d\n", k->n);
k = k ->next 
  }
}
  

